# ------------------------------------------------
Jakub

ALevel
    Implement 
    GetTotalWidth (implicitly formal width), or it could be GetFormalTotalWidth
    GetObservedTotalWidth

ESpectrum
    Adapt to AZURE2, at every point EData is currently used.

# ------------------------------------------------
Kevin

ESpectrum
    Make skeleton
    This object should now hold all of the physical identifiers, i.e. keys, no longer stored in EPoint

EConvolution
    For the latter, make more general for AZURE2

